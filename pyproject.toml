[project]
name = "htmx-kanban"
version = "0.1.0"
description = "A Kanban board built with Flask, SQLModel, HTMX, and _hyperscript"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "sqlmodel>=0.0.37",
]
